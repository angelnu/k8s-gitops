---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${MY_APP}-pvc
spec:
  resources:
    requests:
      storage: "1Gi"
  accessModes:
  - ReadWriteMany
  storageClassName: ${MY_APP}-storageclass
