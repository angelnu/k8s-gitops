---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${MY_APP}-pvc
spec:
  resources:
    requests:
      storage: "1Gi"
  accessModes:
  - ${CEPHFS_ACCESS_MODE:=ReadWriteOnce}
  storageClassName: ${MY_APP}-storageclass
