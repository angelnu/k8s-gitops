---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: cluster-repository
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/angelnu/k8s-gitops.git
  ref:
    branch: main
    # tag: production
  # secretRef:
  #   name: flux-gitea-token
