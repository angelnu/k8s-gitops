kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: vpn-namespace
  namespace: vpn
spec:
  podSelector: {}
  ingress:
  - from: &clusterIPs
    - ipBlock:
        cidr: 172.0.0.0/8
    - ipBlock:
        cidr: 10.0.0.0/8
  egress:
  - to: *clusterIPs
  policyTypes:
    - Ingress
    - Egress
