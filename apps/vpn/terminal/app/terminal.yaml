apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${MY_APP}
  labels:
    app: ${MY_APP}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${MY_APP}
  template:
    metadata:
      labels:
        app: ${MY_APP}
    spec:
      containers:
      - name: alpine
        image: alpine@sha256:865b95f46d98cf867a156fe4a135ad3fe50d2056aa3f25ed31662dff6da4eb62
        command:
        - /bin/sh
        - -c
        - while true; do
          sleep 600 &
          wait $!;
          done