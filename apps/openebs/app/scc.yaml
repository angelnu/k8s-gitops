---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: openebs-hostpath-scc
subjects:
  - kind: ServiceAccount
    name: ${MY_APP}-localpv-provisioner
    namespace: ${MY_NAMESPACE}
  - kind: ServiceAccount
    name: ${MY_APP}-minio
    namespace: ${MY_NAMESPACE}
  - kind: ServiceAccount
    name: ${MY_APP}-system-minio
    namespace: ${MY_NAMESPACE}
roleRef:
  kind: ClusterRole
  name: system:openshift:scc:privileged
  apiGroup: rbac.authorization.k8s.io