 apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: plex
spec:
  values:
    ingress:
      enabled: true
      hosts: [nunez-gregoire.eu]
      tls:
      - hosts:
        - plex.nunez-gregoire.eu