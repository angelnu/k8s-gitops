apiVersion: config.openshift.io/v1
kind: ClusterVersion
metadata:
  name: version
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
    kustomize.toolkit.fluxcd.io/ssa: Merge
spec:
  channel: stable-scos-4
  desiredUpdate:
    version: 4.21.0-okd-scos.1 # renovate: depName=registry.ci.openshift.org/origin/release-scos-next datasource=docker