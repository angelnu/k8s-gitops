apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-kubernetes
spec:
  storageClassName: nfs-kubernetes
  capacity:
    storage: 18T
  accessModes:
    - ReadWriteMany
  mountOptions:
    - local_lock=posix
    - nfsvers=4.1
  nfs:
    server: nas.home.angelnu.com
    path: "/volume1/kubernetes"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-kubernetes
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-kubernetes
  volumeName: nfs-kubernetes
  resources:
    requests:
      storage: 1T