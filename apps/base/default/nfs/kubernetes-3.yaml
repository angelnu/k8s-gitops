apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-kubernetes-3
spec:
  storageClassName: nfs-kubernetes-3
  capacity:
    storage: 18T
  accessModes:
    - ReadWriteMany
  mountOptions:
    - local_lock=posix
    - nfsvers=4.1
  nfs:
    server: nas.home.angelnu.com
    path: "/volume1/kubernetes"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-kubernetes-3
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-kubernetes-3
  volumeName: nfs-kubernetes-3
  resources:
    requests:
      storage: 1T